# Тестирование Forum CRUD API

## Быстрый старт

### 1. Запустить сервер
```bash
cd server
npm run start:dev
```

### 2. Получить токен авторизации

Сначала залогиньтесь как тренер (только тренеры могут создавать форумы):

```bash
curl -X POST http://localhost:3000/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{
    "email": "trainer@example.com",
    "password": "password123",
    "role": "trainer"
  }'
```

Сохраните `accessToken` из ответа.

### 3. Тестовые запросы

#### Получить все форумы (без токена)
```bash
curl http://localhost:3000/api/forum/all
```

#### Получить один форум (без токена)
```bash
curl http://localhost:3000/api/forum/1
```

#### Создать форум (с токеном)
```bash
curl -X POST http://localhost:3000/api/forum \
  -H "Authorization: Bearer YOUR_ACCESS_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "title": "Тестовый форум",
    "description": "Описание тестового форума",
    "category_id": 1
  }'
```

#### Обновить форум (с токеном, только автор)
```bash
curl -X PUT http://localhost:3000/api/forum/1 \
  -H "Authorization: Bearer YOUR_ACCESS_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "title": "Обновленное название",
    "description": "Обновленное описание"
  }'
```

#### Удалить форум (с токеном, только автор)
```bash
curl -X DELETE http://localhost:3000/api/forum/1 \
  -H "Authorization: Bearer YOUR_ACCESS_TOKEN"
```

#### Закрепить форум
```bash
curl -X PUT http://localhost:3000/api/forum/1/toggle-pin \
  -H "Authorization: Bearer YOUR_ACCESS_TOKEN"
```

#### Изменить статус форума
```bash
curl -X PUT http://localhost:3000/api/forum/1/status \
  -H "Authorization: Bearer YOUR_ACCESS_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "status": "closed"
  }'
```

## Проверка каскадного удаления

### 1. Создайте форум
```bash
curl -X POST http://localhost:3000/api/forum \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "title": "Форум для удаления",
    "description": "Тестируем каскадное удаление",
    "category_id": 1
  }'
```

Запомните `id` созданного форума.

### 2. Добавьте комментарии через клиент
Откройте клиент и добавьте несколько комментариев к форуму.

### 3. Проверьте статистику
```bash
curl http://localhost:3000/api/forum/FORUM_ID
```

Вы увидите количество комментариев, лайков и просмотров.

### 4. Удалите форум
```bash
curl -X DELETE http://localhost:3000/api/forum/FORUM_ID \
  -H "Authorization: Bearer YOUR_TOKEN"
```

### 5. Проверьте результат
Ответ должен содержать:
```json
{
  "message": "Форум успешно удален вместе с X комментариями, Y лайками и Z просмотрами"
}
```

### 6. Убедитесь что форум удален
```bash
curl http://localhost:3000/api/forum/FORUM_ID
```

Должна вернуться ошибка 404.

## Проверка прав доступа

### Попытка обновить чужой форум
1. Создайте форум от имени одного тренера
2. Залогиньтесь как другой тренер
3. Попробуйте обновить форум

Должна вернуться ошибка 403 Forbidden.

### Попытка удалить чужой форум
1. Создайте форум от имени одного тренера
2. Залогиньтесь как другой тренер
3. Попробуйте удалить форум

Должна вернуться ошибка 403 Forbidden.

## Ожидаемые результаты

✅ Получение всех форумов работает без авторизации
✅ Получение одного форума работает без авторизации
✅ Создание форума требует авторизации
✅ Обновление форума доступно только автору
✅ Удаление форума доступно только автору
✅ При удалении форума удаляются все комментарии, лайки и просмотры
✅ Закрепление форума работает
✅ Изменение статуса форума работает

## Логи сервера

При выполнении операций в консоли сервера вы увидите:

```
🔄 Создание форума: { title: '...', description: '...', ... }
✅ Форум создан: 1

🔄 Обновление форума: 1 { title: '...' }
✅ Форум обновлен: 1

🔄 Удаление форума: 1
✅ Удалено комментариев: 5
✅ Удалено лайков: 10
✅ Удалено просмотров: 100
✅ Форум удален: 1
```
