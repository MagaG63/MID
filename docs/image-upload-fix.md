# Исправление загрузки и отображения изображений

## Проблема

При регистрации тренера изображения загружались на сервер, но не отображались на клиенте из-за неправильных URL.

## Причины

1. **Относительные URL**: Сервер возвращал относительные пути типа `/uploads/profile/image.jpg`
2. **Неправильный базовый URL**: Клиент пытался загрузить изображения с `http://localhost:5173/uploads/...` вместо `http://localhost:3000/uploads/...`
3. **Дублирование маршрутов**: В App.tsx был дублирован маршрут `/profile` для user и trainer

## Решение

### 1. Исправлены URL на сервере

**server/src/auth/auth.controller.ts**
```typescript
// Теперь возвращаем полные URL
trainerData.profileImage = `http://localhost:3000/uploads/profile/${files.profileImage[0].filename}`;
trainerData.qualificationImages = files.qualificationImages.map(
  (file) => `http://localhost:3000/uploads/qualifications/${file.filename}`,
);
```

### 2. Создана утилита для работы с изображениями

**client/src/shared/lib/imageUtils.ts**
- `getImageUrl(url)` - преобразует относительный URL в абсолютный
- `getImageUrls(urls)` - преобразует массив URL
- `handleImageError(e, fallbackUrl)` - обработчик ошибок загрузки

### 3. Добавлена переменная окружения

**client/.env**
```
VITE_API_URL=http://localhost:3000
```

### 4. Исправлена обработка данных в slice

**client/src/entities/trainer/model/trainer.slice.ts**
- Добавлен парсинг `qualificationImages` если приходит как строка
- Правильное преобразование в формат `FileInfo[]`

### 5. Исправлены маршруты

**client/src/app/App.tsx**
- Убран дублирующий маршрут `/profile`
- Теперь `/profile` доступен всем авторизованным пользователям

## Как это работает

### Регистрация тренера

1. Клиент отправляет FormData с файлами на `/api/auth/register-trainer`
2. Сервер сохраняет файлы в `server/uploads/profile/` и `server/uploads/qualifications/`
3. Сервер возвращает полные URL: `http://localhost:3000/uploads/profile/1234567890-photo.jpg`
4. Клиент сохраняет эти URL в Redux store

### Отображение изображений

1. Компонент MyPage.tsx получает данные из Redux store
2. Утилита `getImageUrl()` проверяет URL:
   - Если абсолютный (начинается с http) - возвращает как есть
   - Если относительный - добавляет `http://localhost:3000`
3. Изображение загружается с правильного URL
4. При ошибке загрузки показывается placeholder

## Структура URL

### Профильное изображение
```
http://localhost:3000/uploads/profile/1234567890-photo.jpg
```

### Сертификаты
```
http://localhost:3000/uploads/qualifications/1234567890-cert1.jpg
http://localhost:3000/uploads/qualifications/1234567891-cert2.jpg
```

## Проверка работы

### 1. Проверка загрузки файлов
```bash
# Проверьте, что файлы сохранились на сервере
ls server/uploads/profile/
ls server/uploads/qualifications/
```

### 2. Проверка доступности файлов
Откройте в браузере:
```
http://localhost:3000/uploads/profile/имя_файла.jpg
```

### 3. Проверка в консоли браузера
При входе тренера должны быть логи:
```
✅ LOGIN RESPONSE: { user: { profileImage: "http://localhost:3000/uploads/...", ... } }
📦 Trainer data before parse: { profileImage: "http://localhost:3000/uploads/...", ... }
✅ Преобразованный профиль тренера: { profileImage: "http://localhost:3000/uploads/...", ... }
📊 Profile debug: { profileImage: "http://localhost:3000/uploads/...", ... }
```

### 4. Проверка в Network DevTools
- Откройте DevTools -> Network
- Перейдите на страницу профиля
- Должны быть успешные запросы к `http://localhost:3000/uploads/...` со статусом 200

## Возможные проблемы

### Изображения не загружаются

**Проблема**: 404 ошибка при загрузке изображений

**Решение**:
1. Проверьте, что сервер запущен на порту 3000
2. Проверьте, что файлы существуют в папке `server/uploads/`
3. Проверьте CORS настройки в `server/src/main.ts`

### Изображения показывают placeholder

**Проблема**: Вместо изображений показывается placeholder

**Решение**:
1. Откройте консоль браузера и проверьте ошибки
2. Проверьте URL изображения в Redux DevTools
3. Попробуйте открыть URL изображения напрямую в браузере

### Относительные URL

**Проблема**: URL изображений начинаются с `/uploads/` вместо `http://localhost:3000/uploads/`

**Решение**:
1. Проверьте, что в `auth.controller.ts` используются полные URL
2. Проверьте, что утилита `getImageUrl()` правильно преобразует URL
3. Перезапустите сервер

## Production настройки

Для production нужно:

1. Использовать переменные окружения:
```typescript
// server
const baseUrl = process.env.BASE_URL || 'http://localhost:3000';
trainerData.profileImage = `${baseUrl}/uploads/profile/${filename}`;
```

2. Настроить клиент:
```env
# .env.production
VITE_API_URL=https://your-domain.com
```

3. Настроить CDN или S3 для хранения изображений (опционально)

## Дополнительные улучшения

### Оптимизация изображений
- Добавить сжатие изображений при загрузке
- Генерировать thumbnails для превью
- Использовать WebP формат

### Безопасность
- Валидация типов файлов
- Ограничение размера файлов
- Проверка на вирусы

### Производительность
- Lazy loading изображений
- Кэширование на клиенте
- CDN для статических файлов
