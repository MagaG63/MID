# Исправление ошибки 404 при обновлении профиля тренера

## Проблема

При попытке обновить профиль тренера возникала ошибка 404:
```
PUT /api/trainer/profile -> 404 Not Found
```

## Причина

В `TrainerModule` не был зарегистрирован `TrainerController`, поэтому маршруты контроллера не были доступны.

## Решение

### Было (неправильно):
```typescript
// server/src/trainer/trainer.module.ts
@Module({
  imports: [SequelizeModule.forFeature([Trainer])],
  providers: [TrainerService],
  exports: [TrainerService],
})
export class TrainerModule {}
```

### Стало (правильно):
```typescript
// server/src/trainer/trainer.module.ts
import { TrainerController } from './trainer.controller';

@Module({
  imports: [SequelizeModule.forFeature([Trainer])],
  controllers: [TrainerController], // ✅ Добавлено
  providers: [TrainerService],
  exports: [TrainerService],
})
export class TrainerModule {}
```

## Дополнительные исправления

### 1. Экспортирован тип SafeTrainerData

```typescript
// server/src/trainer/trainer.service.ts
export interface SafeTrainerData { // ✅ Добавлен export
  id: number;
  name: string;
  email: string;
  // ...
}
```

### 2. Добавлено логирование в trainer.api.ts

```typescript
static async updateProfile(formData: FormData): Promise<TrainerApiResponse> {
  console.log('📤 Отправка обновления профиля тренера на /api/trainer/profile');
  
  // Логируем содержимое FormData
  for (const [key, value] of formData.entries()) {
    console.log(`🔍 FormData поле: ${key} =`, value);
  }
  
  // ...
}
```

## Проверка

После исправления перезапустите сервер:

```bash
cd server
npm run start:dev
```

Проверьте, что маршрут зарегистрирован:
- Сервер должен запуститься без ошибок
- В логах не должно быть ошибок о несуществующих маршрутах

## Тестирование

1. Войдите как тренер
2. Перейдите на страницу профиля
3. Нажмите "Редактировать профиль"
4. Измените данные
5. Нажмите "Сохранить"
6. ✅ Профиль должен обновиться без ошибки 404

## Доступные маршруты

После исправления доступны следующие маршруты:

### Trainer
- `POST /api/trainer/register` - регистрация тренера
- `PUT /api/trainer/profile` - обновление профиля тренера ✅

### User
- `POST /api/user/registration` - регистрация пользователя
- `PUT /api/user/profile` - обновление профиля пользователя ✅

### Auth
- `POST /api/auth/register` - регистрация пользователя
- `POST /api/auth/register-trainer` - регистрация тренера
- `POST /api/auth/login` - вход
- `POST /api/auth/refresh` - обновление токена
- `DELETE /api/auth/logout` - выход

## Структура модулей

Правильная структура NestJS модуля:

```typescript
@Module({
  imports: [
    // Импорт других модулей
    SequelizeModule.forFeature([Model])
  ],
  controllers: [
    // ✅ Контроллеры для регистрации маршрутов
    MyController
  ],
  providers: [
    // Сервисы и другие провайдеры
    MyService
  ],
  exports: [
    // Что экспортируем для использования в других модулях
    MyService
  ],
})
export class MyModule {}
```

## Важно

Всегда проверяйте, что:
1. Контроллер импортирован в модуль
2. Контроллер добавлен в массив `controllers`
3. Модуль импортирован в `AppModule`
4. Сервер перезапущен после изменений

## Отладка 404 ошибок

Если получаете 404:

1. **Проверьте URL**
   - Правильный ли путь?
   - Есть ли префикс `/api`?
   - Правильный ли HTTP метод (GET, POST, PUT, DELETE)?

2. **Проверьте контроллер**
   - Зарегистрирован ли в модуле?
   - Правильный ли декоратор `@Controller('path')`?
   - Правильный ли декоратор метода `@Put('profile')`?

3. **Проверьте модуль**
   - Импортирован ли в AppModule?
   - Есть ли контроллер в массиве `controllers`?

4. **Проверьте логи сервера**
   - Запустился ли сервер без ошибок?
   - Нет ли ошибок компиляции TypeScript?

5. **Проверьте Network в DevTools**
   - Какой URL отправляется?
   - Какой метод используется?
   - Какие заголовки отправляются?
