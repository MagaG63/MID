# Laboratory Client - Инструкция

## ✅ Создана полная структура клиента

### 1. Entities (client/src/entities/laboratory/)
```
laboratory/
├── api/
│   └── laboratory.service.ts      # API запросы к серверу
├── model/
│   ├── laboratory.type.ts         # TypeScript типы
│   ├── laboratory.schema.ts       # Zod схема валидации
│   ├── laboratory.slice.ts        # Redux slice
│   └── laboratory.thunks.ts       # Async thunks
└── index.ts                       # Экспорты
```

### 2. Страница (client/src/pages/Laboratory/)
```
Laboratory/
├── LaboratoryPage.tsx             # Компонент страницы
└── LaboratoryPage.css             # Стили
```

### 3. Интеграция
- ✅ Добавлен `laboratoryReducer` в Redux store
- ✅ Добавлен роут `/laboratory` в App.tsx
- ✅ Добавлена ссылка в NavBar

## Функционал страницы:

### Список лабораторий
- Карточки с name, stated, status
- Кнопка "Подробнее" на каждой карточке
- Адаптивная сетка (responsive grid)

### Модальное окно
- Открывается по клику на "Подробнее"
- Загружает полные данные лаборатории по ID
- Показывает все поля: id, name, stated, status, post, createdAt, updatedAt
- Закрывается по клику вне окна или на крестик

## API запросы:

### GET /api/laboratory
Получает все лаборатории при загрузке страницы

### GET /api/laboratory/:id
Получает детальную информацию при клике на "Подробнее"

## Как запустить:

### 1. Убедитесь что сервер запущен
```bash
cd server
npm run start:dev
```

### 2. Запустите клиент
```bash
cd client
npm run dev
```

### 3. Откройте в браузере
```
http://localhost:5173/laboratory
```

## Навигация:

Ссылка "Лаборатории" добавлена в боковое меню (NavBar) между "Форум" и другими пунктами.

## Готово! 🎉

Страница лабораторий полностью готова к использованию с:
- ✅ Списком всех лабораторий
- ✅ Модальным окном с деталями
- ✅ Redux state management
- ✅ Красивым дизайном
- ✅ Адаптивной версткой
